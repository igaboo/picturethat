import 'package:flutter/material.dart';

/// TODO
/// get photo submission (camera or gallery)
/// get submission description
/// upload photo and doc
/// update state of prompt

class SubmitPhotoScreen extends StatelessWidget {
  const SubmitPhotoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("SubmitPhoto Screen"),
      ),
    );
  }
}
